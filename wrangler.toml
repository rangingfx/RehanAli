name = "rehan-ali-portfolio"
compatibility_date = "2024-09-23"

# Entry point: main = "src/index.tsx" (if deploying with custom worker backend)
# For client-side Single Page Application (SPA), Cloudflare serves directly from 'dist'
[assets]
directory = "dist"
not_found_handling = "single-page-application"

